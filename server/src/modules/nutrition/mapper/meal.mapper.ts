import { FoodInMeal } from "@prisma/client";
import { MealExtendedModel } from "../interfaces/meal.interfaces.js";

export class MealMapper{
    static MealToHTTP(meal:MealExtendedModel){
        return {

        }
    }
}